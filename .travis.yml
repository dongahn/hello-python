language: python
  - "2.7"
  - "3.7"

services:
  - docker

before_install:
  - docker pull dongahn/friendlyhello
  - docker run -p 4000:80 dongahn/friendlyhello
  - docker container ls

install:
  - pip install --user pycotap

script:
  - make check
  - curl localhost:4000

